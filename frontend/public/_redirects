/assets/* /assets/:splat 200
/icons/* /icons/:splat 200
/app-logo.png /app-logo.png 200
/favicon.png /favicon.png 200
/vite.svg /vite.svg 200
/manifest.json /manifest.json 200
/sw.js /sw.js 200
/robots.txt /robots.txt 200
/sitemap.xml /sitemap.xml 200

# NOTE: Do NOT add explicit /faq → /faq.html rewrites here.
# Cloudflare Pages automatically serves faq.html at /faq (pretty URLs).
# Adding a rewrite rule creates a redirect loop because Cloudflare Pages
# also redirects /faq.html → /faq as a canonical redirect.

# SPA fallback — serves root (index.html) for any route not matched by a static file.
# Root-level static files (app-logo.png, favicon.png, vite.svg) MUST be listed
# explicitly above this catch-all, because the `/* / 200` form rewrites any
# unmatched URL to `/` which serves index.html — silently turning root-level
# assets into HTML responses when no explicit rule claims them first.
/* / 200
